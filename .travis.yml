language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install tox-travis
  - pip install codecov
  - pip install jsonschema # needed for check version
script:
  - tox
  - ./check_version.py
after_success:
  - codecov
